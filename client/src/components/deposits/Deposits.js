class Deposits extends Components {
    render() {
        return (`<div class="Deposits" id="deposits" style="display: ${this.blocked}">
            <h1>Page in development...</h1>
        </div>`)
    }
}
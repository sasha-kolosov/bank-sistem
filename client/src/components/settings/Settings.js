class Settings extends Components {
    render() {
        return (`<div class="Settings" id="settings" style="display: ${this.blocked}">
            <h1>Page in development...</h1>
        </div>`)
    }
}
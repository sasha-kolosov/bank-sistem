class Payments extends Components {
    render() {
        return (`<div class="Payments" id="payments" style="display: ${this.blocked}">
            <h1>Page in development...</h1>
        </div>`)
    }
}